# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: asl_sign.yaml
  - override /model: asl_sign.yaml
  - override /callbacks: default.yaml
  - override /trainer: ddp.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["asl_sign"]

seed: 42

trainer:
  log_every_n_steps: 1
  check_val_every_n_epoch: 1
  precision: 16
  min_epochs: 0
  max_epochs: 200
  devices:
  - 0
  - 1

data:
  batch_size: 500
  num_workers: 4
  pin_memory: True
  persistent_workers: True
  dataset_cfg:
    fold_type: hwc
    val_fold: 3.0
    max_length: 512
    random: False
    use_feature: False

model:
  net:
    num_blocks: 1
    num_classes: 250
    num_head: 4
    embed_dim: 512
    max_length: ${data.dataset_cfg.max_length}
    num_point: 82
  


desc: "transformer_length_${data.dataset_cfg.max_length}_${model.scheduler.sch_name}_${data.dataset_cfg.fold_type}_random_${data.dataset_cfg.random}_fold${data.dataset_cfg.val_fold}"
experiment_uuid: ${uuid:}
experiment_name: exp_${experiment_uuid}_${desc}

logger:
  wandb:
    project: asl-sign-detection-kaggle
    name: ${experiment_name}