_target_: src.data.asl_sign_datamodule.ASLDataModule

batch_size: 64
num_workers: 1
pin_memory: False
dataset_cfg:
  train_path: ${paths.data_dir}/csv/train.csv
  pr_train_path: ${paths.data_dir}/csv/train_prepared.csv
  json_path: ${paths.data_dir}/sign_to_prediction_index_map.json
  num_fold: 5
  fold_type: christ
  val_fold: 0.0
  max_length: 60
  random: False
